# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# venv
.venv/
venv/

# benchmark / run artifacts
*.csv
*.png
results/
*.nsys-rep
*.sqlite
benchmarks/compare/*.json

# model weights / caches (never commit — pulled from HF hub)
# 注意:不要用 `models/` 这种目录规则,会误伤 omniserve/models/ 代码目录;
# 权重是 *.safetensors / *.bin,按扩展名排除即可。
*.safetensors
*.bin
.cache/

# editor / OS
.vscode/
.idea/
.DS_Store
