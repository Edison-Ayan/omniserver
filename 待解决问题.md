# 攻坚记录 / 下一步

> **更新(2026-06-16)**:这份文档原本是「把 omniserve 从 0.44x of vLLM 往上推」的 TODO 清单
> (P1 FlashAttention / P2 CUDA graph / P3 量化 GEMM)。**P1、P2、P3 都做完了**,而且把基准
> 测公平之后,结论整个变了——fp16 真冷 omniserve 反而比 vLLM **快 1.28x**,int4 同精度追到
> **0.82x**。所以本文档从「待办」改成「攻坚记录 + 真正剩下的事」。详见 OPTIMIZATION.md(§6–§9
> + The attribution)和 探索日志.md 第 8–17 阶段。
>
> **当前主攻方向(2026-06-17 立项):P5 混合精度量化**(见下文 P5 + 探索日志.md 第 18 阶段)。

复利链当初的判断(nsys):vLLM 的快是层层相乘的——

```
  P1 FlashAttention ──► P2 CUDA graph(变 launch-bound 才有用)
        │              └─► P4 varlen/混合 prefill(需要 cu_seqlens)
        └─► 顺带干掉 19% KV 物化
  P3 量化 GEMM(独立的另一条线:减权重字节)
```

这条链当初判定「第一环不解决后面白做」。事后看:**链确实是对的,而且我们把前三环都走通了**;
只是终点不是「逼近 vLLM」,而是发现 fp16 下我们本就更快、差距只在 int4 才显形(见归因)。

---

## ✅ P1. FlashAttention —— 已完成(OPTIMIZATION §6)

不自己编译、不撞 nvcc/cu130 的墙:**直接 import vLLM 自带、和本机 torch ABI 匹配的 flash 二进制**
(`vllm.vllm_flash_attn`),零编译。decode 用 flash-decoding(KV 池槽位当一个 paged block,
`block_table` + `seqused_k` 直接读池、不 gather、GQA-native、不物化 KV),prefill 用 varlen。
micro-bench flash vs SDPA+物化 **11.56x**;引擎内 decode 29.6→19.65ms(1.5x)。**19% 的 GQA KV
物化消失。** 复用生产二进制而非和编译器搏斗,是全项目 ROI 最高的一步。

## ✅ P2. CUDA graph 捕获 decode —— 已完成(OPTIMIZATION §7–§8)

当初判「decode GPU-bound,graph 无用(back-to-back/synced=1.02)」。**这个结论被 P 后续推翻。**
先修两处 omniserve 自己的低效:
- **§7 decode-copy fix**:`flash_decode` 每层把整个 `max_len=1024` 的 KV transpose+contiguous,
  实际才 ~300 → 只拷 `ret_len`(16 倍数)。decode **16.4→12.6ms**,顺带证伪了「那 4ms 是 vLLM
  kernel 护城河」——是我们自己的拷贝浪费。
- **§8-A KV layout**:池从 `[B,nkv,max_len,hd]` 改成 `[B,max_len,nkv,hd]`(正好是 flash 的 block
  布局,免每层 transpose),写入改单维 `index_copy_`。decode **12.6→11.5ms**。

修完后即便 CPU 跟得上,**GPU 侧 28 层 kernel 之间仍有调度间隙** → CUDA graph 把间隙吃掉,decode
**11.5→9.5ms(1.2x)**。整条 decode 链 **16.4→9.5ms = 1.72x**,token 一致。

> **★调试坑(以后做 CUDA graph 必看)**:反复 illegal memory access。`mg.py` 隔离 capture
> 单层 / 完整 forward 都 OK → 问题在 engine 集成。真凶:**capture 用的 `view` 是局部变量,函数
> 返回后被 GC**,它被 captured graph 引用的内部 tensor(`_rows` 等)内存被复用 → replay illegal
> access。修复:`self._graphs[n] = (g, buf, view)` 保活。另两坑:① 依赖 buffer 的索引
> (`flat`/`seqused`)必须图外预算成 static buffer;② 不能在 `@torch.inference_mode()` 内 capture。

## ✅ P3. 量化 GEMM(Marlin / GPTQ)—— 已完成(OPTIMIZATION §9)

作为**可选扩展**(默认仍 fp16,公平对比恒 fp16-vs-fp16)。为了和 **vLLM-int4 同精度**对比(不是
int4-vs-fp16 的便宜),omniserve **加载 vLLM 同款生产 GPTQ-Int4 权重**、`gptq_marlin_repack`
成 Marlin 跑(`omniserve/kernels/marlin_linear.py`)。同精度结果:真冷 421 vs 453(0.93x),
reuse0.9 **1785 vs 2186(0.82x)**——经 §7/§8 从 0.54x 一路追上来。

> 坑:① 每层两个 RMSNorm 必须加载(留初始化全 1 → 整体乱码,proj 单测却全过,逐层二分定位);
> ② **bias 不能传进 `gptq_marlin_gemm`**(结果错 rel 0.90),要 kernel 外手动加(rel 0.0002)。

**自家在线 RTN 只能量化 MLP**(qkv/o 对量化敏感,RTN 会乱码且小 GEMM 本就不快);要全 decoder
同精度必须用校准过的 GPTQ 权重。

---

## ✅ P4. varlen 打包 prefill —— 已完成(2026-06-21 验证收口)

**当初状态(06-16)**:打包 prefill 的调度预算 + PackedPrefillCache + block-diagonal mask 写好了
(token 一致),但注意力是 O(total²) SDPA,实测 0.97x(没收益)。「就差把注意力换成 varlen flash」。

**事后**:那一步在 P5 量化冲刺期间(06-17~20)顺手做掉了,只是本文档没更新。现状是
**已实现并接进线上**:
- `cache/kv_prealloc.py::_PackedPrefillWriter.flash_prefill`——用 `flash_attn_varlen_func`
  + `cu_seqlens` 只算段内因果(O(sum L²),不再 O(total²));各段 KV 直接写池槽位。
- `model/qwen2_llm.py` 注意力:cache 有 `flash_prefill` 就走 varlen,`attn_mask=None`(段隔离靠
  cu_seqlens,不再需要块对角 mask)。
- `runners/multimodal.py::prefill`:多序列 `cat(embeds, dim=1)` + `seg_lens` 打包成一次前向;
  `NativeQwenVLRunner` 继承之,benchmark(pareto/run_omniserve)设 `max_prefill_per_step=max_running`
  一直在用。**注意**:`server.py`/`profile_engine.py` 仍是 `max_prefill_per_step=1`(没开打包)。

**隔离 A/B 验证(2026-06-21,`benchmarks/bench_packed_prefill.py`)**:纯文本(绕开 ViT)、
max_new_tokens=1(只测 prefill)、内容各异的 K 个 prompt,打包(varlen 一次前向)vs 逐序列。
```
  K=8  短(段长30–90,total 444)  : packed 67ms  vs seq 155ms  = 2.30x
  K=16 短(total 888)            : packed 122ms vs seq 314ms  = 2.58x
  K=8  长(段长90–300,total 1560): packed 210ms vs seq 288ms  = 1.37x
```
**正确性**:内容各异 → 各段首 token 不同 `[32,785,785,49896,82280,...]`,packed 与 sequential
**逐位完全一致** → varlen 段间隔离正确。**加速比随 K 增大、随段长减小**(短 prompt 时每步
调度/launch 开销占比大,打包摊薄最明显;长 prompt 时 GEMM 主导,收益收敛到 ~1.4x 但仍 >1)。
这把 06-16 记的「O(total²) SDPA 0.97x」正式翻成真收益。

---

## ✅ P4-下篇 chunked prefill —— 已完成(2026-06-21)

**问题**:每步是「prefill 整段 → decode 各吐一个」两次独立前向。一个长 prompt 到达时,它那步
把整段一次算完,正在流畅吐字的 decode 序列干等 → ITL 尖峰(stall)。基准(`bench_chunked_prefill.py`):
6 路 decode 中注入 620 token 长 prompt,注入步 98ms vs 稳态 15.5ms = **6.3x 尖峰**;920 token 时
**9.45x**。

**做法(Phase A,加法式、零回归)**:长 prompt 按每步 `max_prefill_tokens` 预算切块,逐块 append
到一个**保留槽位 R**(= max_batch-1,和 decode 区 [0,n_dec) 完全隔离),算完最后一块再一次性
`move_slot` 进 decode 区转为普通 decode 序列。期间每步只算一块、decode 照常推进不被堵。
- `cache/kv_prealloc.py::_ChunkedPrefillWriter.flash_prefill`:**append 注意力**——本块 KV 写
  pool[slot, start:start+L],query 用 `block_table`+`seqused_k` 注意到 [0:start+L](prefix+本块,
  causal)。**decode(L=1)和 prefill-chunk(L=任意)本质同一个 append 注意力**,flash 的 causal
  在 q_len<k_len 时自动右下对齐 → query i 注意 [0, start+i],正好。
- `scheduler.py`:只有 `prompt_len > budget` 才切块(短 prompt 仍走整段打包路径 → pareto/P4 零
  回归,预算 16384 时永不触发);切块的长 prompt 独占该步、不和短 prompt 打包;`Sequence.num_prefilled`
  记进度。`engine.py` 加 `prefill_chunk` 分支;decode/打包 prefill 两条热路径**没动**。
- runner 多预留 1 槽位给在飞的 chunk,decode 用 `view(n_dec)` 看不到它。

**实测(`bench_chunked_prefill.py`,同进程 warmup 后 A/B)**:
```
  620 tok, chunk=256: 峰值步 98ms → 56ms  = 1.75x 削平(分 3 块)
  920 tok, chunk=128: 峰值步 147ms → 39ms = 3.74x 削平(分 8 块)
```
**正确性**:长 prompt 输出 token 与整段 prefill **逐位完全一致**(append 注意力 + 切块位置/rope
正确)。**旋钮**:chunk 越小峰值越低(代价是步数多、总 prefill 略增)。回归检查:P4 打包基准
2.39x 不变、StubRunner 控制面 7 序列正常。

**MVP 边界**:① 纯文本长 prompt(块内含图像 token 的暂不切,有图 prompt 回退整段);② 一次只
分块一个长 prompt(后来的短 prompt 会在它身后等 → 轻微 HoL);③ prefill 块和 decode 仍是**两次
独立前向**。

**还没做(Phase B,mixed batching)**:把"这步的 prefill 块"和"decode 的 token"塞进**同一次
前向**(一个 GEMM + 一次 append 注意力,cu_seqlens_q 标各序列 token 数:decode=1、chunk=L)。
省掉两次 launch、把 decode 的小 M GEMM 喂饱。append 注意力已统一(decode 是 L=1 特例),主要是
合并 embed/positions + 调度让 decode 与 chunk 同批。属独立优化,非必需。

---

## ⭐ P5. 混合精度量化研究 —— 当前主攻方向(2026-06-17 立项)

**thesis**:最优配置不是全局单精度,而是 **roofline(带宽 vs 算力,由 GEMM 的 M 维决定)⊕
数值敏感度** 的函数。第 12–13 阶段已证「统一 int4 超不过 vLLM」,根因是 vLLM 本就是**按阶段/算子
混合精度**;下一步把「**每个 GEMM 用什么精度**」做成一等公民。详细原理推演见 探索日志.md 第 18 阶段。

**两条钥匙**:① 权重精度 W(静态,只省带宽)与激活精度 A(动态,要在线量化、省带宽+算力但有
固定开销)是两个独立轴;② decode 带宽受限→W4A16 香、prefill 算力受限→W8A8/FP8 香、小 GEMM
(qkv/o)形状+敏感度双重原因→保 fp16。

**已有出发点**(第 11–13 阶段已测,decode M=24):gate_up int4 **2.42x**、down 1.61x、qkv/o
**0.85–0.96x**;FP8 小 GEMM **0.26x**。**缺口:prefill 大 M 那一列没测。**

**子任务(按 ROI 排)**:
1. ✅ **补 micro-bench 的 prefill 大 M 列 + 真实模型复测**(2026-06-19 完成,
   `benchmarks/bench_gemm.py` 随机权重测速 + `benchmarks/bench_gemm_real.py` 真实权重×真实激活)。
   **假设1 成立(真实权重再验)**:gate_up M=512 FP8 2.16x vs W4A16 1.11x、M=1024 1.78x vs 1.11x。
   W4A16 prefill 退化到 ~fp16(只省带宽不省算力),FP8 吃 2x 算力才是 prefill 的解。gate_up 是 FP8
   通吃(连 decode M=24 都 3.28x>2.45x)。**真实精度损失**(rel L2,layer 2/14 一致):FP8 0.015–0.035
   ≈ 无校准 RTN-int4(0.046–0.107)的 1/3;o_proj/down(SwiGLU)是 RTN 最难的两层。**rowwise per-token
   在文本激活上几乎无改善 → 先别上**。诚实边界:激活是纯文本(图文 outlier 可能更强,待测)、RTN≠GPTQ、
   单 GEMM 是 proxy(端到端 token 接受率另测)。详见 探索日志.md 第18阶段两节实测。
   → 导出的 per-op 雏形:gate_up=FP8;qkv/o=fp16;decode-MLP=W4A16;down 按阶段。
2. 🔨 **选择性加载 / per-op 精度配置**(进行中,2026-06-19):
   - ✅ **第一块积木**:发现 P3 阶段(commit ab40bd4)早已写过 `kernels/fp8_linear.py` 的 `FP8Linear`
     (Triton 融合激活量化 + RowWise),但**当时定位搞反**(docstring 说"砍 decode 带宽"),且
     **从没被 import/集成/验证**。本轮:验证它跑通(rel ~0.037、多维 reshape OK、含 bias 正确)、
     **修正 docstring 认知(decode→prefill 甜区)**、加 `FP8Linear.from_linear` 统一接口、加
     `apply_fp8(model, scope=gate_up/mlp/decoder)` 接入(对齐 `quantize_llm_marlin`)。
   - ✅ **第二块积木**(2026-06-19):`kernels/mixed_precision.py` 的 `apply_mixed(model, plan)`——
     可配置 per-op 精度计划(fp16/fp8/marlin 三档自由组合),复用 FP8Linear + MarlinInt4Linear。
     默认 plan = MLP(gate_up/down)=FP8 + attention(qkv/o)=fp16。接进 Qwen2VLAdapter 的
     `quant="mixed"`(+ run_omniserve.py `--quant mixed`),默认 fp16 不变、可选开启。
   - ✅ **第三块 初步端到端**(8 req,native):mixed **266 vs fp16 244 tok/s = 1.09x**,
     **峰值显存 5304→4246 MiB(省 1058 MiB/20%,= MLP 权重 fp16→FP8 减半,和理论吻合)**。
     token:语义保持(形状/颜色对、无乱码),但 greedy 自回归会分叉、个别样本轻微降质
     (Ellipse→Oval)——lossy 量化预期。**显存省 20% 对 8GB 4060 受限主题是强卖点。**
   - ✅ **第四块 帕累托收口**(2026-06-19,`benchmarks/compare/pareto.py` → `pareto.png`):
     速度(native 引擎端到端)× 质量(teacher-forcing 下对 fp16 的 token 接受率 + ppl,走原生
     cache=None 全序列 SDPA)。16 req 实测:
     ```
     config   tok/s  质量损失%   ppl    显存MiB
     fp16     340.5    0.00    72.15    5037
     mixed    354.8    5.72    75.33    4022   ← 低质量损失区唯一点
     marlin   366.3   20.43    78.92    3400   ← 被 gptq 三维全支配,出局
     gptq     393.5   19.76    78.87    3187   ← 速度/显存最优点
     ```
     **结论**:① 无全局最优精度,是帕累托曲线 {fp16, mixed, gptq},按可容忍质量损失选点;
     ② **mixed 开辟 int4 达不到的低损失区(5.7% vs ~20%,1/4)**,代价是速度/显存略逊 gptq;
     ③ **marlin(RTN int4)被 gptq 完全支配**(质量/速度/显存三维全输)→ 要 int4 就用校准 gptq,别用 RTN;
     ④ 同样只量 MLP,**FP8(mixed 5.7%)质量比 RTN-int4(marlin 20.4%)好 3.5x**,端到端印证 micro-bench
     的 FP8 rel 0.03 vs RTN 0.10。诚实边界:速度 trials=2 有噪声、质量纯文本 teacher-forcing、ppl 绝对值
     高(技术说明文)。
   - ✅ **Hadamard 旋转 已 measure(2026-06-20,`benchmarks/bench_hadamard.py`)**:块对角 Hadamard
     (块 256)对真实激活的量化误差。结论:① **对 FP8 零增益**(down rel 0.0296→0.0299)——纯文本
     outlier 不重,FP8 范围够大,**当前 mixed/FP8 不该引入 Hadamard**;② **W4A4 直接乱码**(rel 0.32–0.56),
     Hadamard 救回 1.2–2.9x(**down 最显著 0.56→0.20,SwiGLU 中间激活 outlier 最重**);③ 但 W4A4+H
     (0.15–0.26)**仍远输 FP8(0.03)** → 除非要 W4A4 的极致 int4 激活带宽,否则 FP8 更优,Hadamard
     在本路线不值得。**用数据避免无用功:停在 FP8。** 边界:纯文本(图文 outlier 可能翻盘,见下 ③)。
   - ✅ **Hadamard 已工具化(2026-06-20)**:`kernels/hadamard.py`(块对角旋转,计算不变 y=(aH)(WH)ᵀ);
     `FP8Linear`/`MarlinInt4Linear` 加 `hadamard=False` flag(权重离线吸收 W·H + 激活在线旋转);
     `mixed` plan 支持 `+h` 后缀(如 `fp8+h`)。默认关,"必要时"才开。验证:FP8+H 计算不变性正确
     (rel 0.037 仍接近 fp16)。**附带发现:`marlin+h`(W4A16)反而变差(0.070→0.112)**——Hadamard 打散
     权重 per-group 局部结构,而 W4A16 激活 fp16、无激活 outlier 可救 → **Hadamard 只帮激活量化
     (FP8/W4A4),不帮纯权重量化**;`marlin+h` 保留但默认不用。
   - ✅ **A2 图文激活复测(2026-06-20,`bench_hadamard.py --multimodal`)**:抓真实图文激活(走 ViT,
     含图像 token,layer 14 M=287)对比纯文本。结论:**图文 outlier 并不比纯文本明显更重**——
     ① 粒度 gap(per-tensor/per-row)图文 ~1.02x = 纯文本(outlier 重则会拉到 1.5x+);
     ② FP8+Hadamard 图文仍零增益(down 0.0326→0.0329)**不翻盘**;③ FP8 误差图文仅 +10–15%
     (0.030→0.033)仍 ~0.03 可用。**→ 所有纯文本量化结论在真实多模态上依然成立,mixed(FP8)稳健,
     Hadamard 价值始终只在 W4A4(图文救回仍 2.68x)。** 整条 Hadamard/outlier 探索收口:停在 FP8,
     Hadamard 工具留给 W4A4,不进默认路径。**真实照片复确认**(1024px,M=1026 大量 image token):
     gap 1.02x、FP8+H 仍零增益(down 0.0326→0.0331)、W4A4 救回 2.77x —— 纯文本/合成图/真实照片
     三场景结论一致。边界:单层(layer 14)。
   - ⏳ **下一步(可选,均非必需)**:① 阶段混合(decode 偏 W4A16、prefill 偏 FP8,同算子按 M 切);
     ② mixed 再加 gptq 的 qkv/o(补帕累托中段);③ 端到端图文 token 接受率(当前 A2 是 GEMM 误差,
     pareto.py 质量轴仍纯文本)。
3. **阶段混合**:decode W4A16(Marlin)+ prefill FP8(W8A8)——验证端到端优于任一单配置。
4. **产出帕累托图**:横轴质量损失(对 fp16 的 token 接受率 / perplexity),纵轴吞吐,点=不同
   精度配置。这是本方向的核心交付物。
5.(可选)SmoothQuant 治 W8A8 激活 outlier;AWQ 对比 GPTQ;per-token/group 粒度权衡。

**验证**:token/质量双轨——正确性(量化 vs fp16 偏差)+ 速度(per-GEMM micro-bench + 端到端公平
基准,冷测全新图)。

**sm_89 FP8 别搞反**:甜区是 **prefill 大 GEMM(算力受限)**,不是 decode(带宽受限,FP8 1B 不如
int4 0.5B)。

**诚实定位(简历)**:不是「量化追平 vLLM」(已证 kernel 护城河追不平),而是「**用 per-GEMM × 阶段
× 敏感度的混合精度做出 measure-driven 的速度/质量帕累托,并用 roofline 解释每个算子为何选该精度**」。

---

## 已不再追的(带数据的「不做」决定)

- **fused decode kernel(norm+rope+residual+quant 融合成更少 kernel)**:这是 int4 decode
  9.5ms vs vLLM ~7–8ms 那最后一截差距的来源,纯 vLLM 几十人年 kernel 工程。measure 已证
  `VLLM_MARLIN_USE_ATOMIC_ADD`、再加 graph 都不再帮——收益不值在 8GB 上复刻。
- **重叠批量 prefill(批 ViT + prefill/decode 流水)**:int4 真冷 vLLM 290→453 靠这个,
  omniserve 卡在串行逐图 ViT。同属 vLLM 引擎成熟度,不追。
- **lean C++ forward**:一次重写,相对上面收益边际,不做。

## 归因(本项目最有价值的判断,见 OPTIMIZATION.md「The attribution」)

差距不在固定一层,**随精度迁移**:
- fp16:decode 卡**带宽 floor**(硬件、两边平等)、prefill 卡 **ViT**(两边接近)→ omniserve
  精简路径**赢**。
- int4:权重读取掉到 1/4,瓶颈从带宽换成 **kernel 调度效率**(vLLM 护城河);prefill 被 vLLM
  量化 LLM + 重叠吃满 → omniserve **被反超**。

**量化没让 omniserve 变慢,是让 vLLM 的工程优势变可见。** 能指出差距在哪一层、为什么随精度移动、
要什么(融合 kernel + prefill 重叠)才能补——比刷一个数字强。
