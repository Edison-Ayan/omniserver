# 攻坚记录 / 下一步

> **更新(2026-06-16)**:这份文档原本是「把 omniserve 从 0.44x of vLLM 往上推」的 TODO 清单
> (P1 FlashAttention / P2 CUDA graph / P3 量化 GEMM)。**P1、P2、P3 都做完了**,而且把基准
> 测公平之后,结论整个变了——fp16 真冷 omniserve 反而比 vLLM **快 1.28x**,int4 同精度追到
> **0.82x**。所以本文档从「待办」改成「攻坚记录 + 真正剩下的事」。详见 OPTIMIZATION.md(§6–§9
> + The attribution)和 探索日志.md 第 8–17 阶段。

复利链当初的判断(nsys):vLLM 的快是层层相乘的——

```
  P1 FlashAttention ──► P2 CUDA graph(变 launch-bound 才有用)
        │              └─► P4 varlen/混合 prefill(需要 cu_seqlens)
        └─► 顺带干掉 19% KV 物化
  P3 量化 GEMM(独立的另一条线:减权重字节)
```

这条链当初判定「第一环不解决后面白做」。事后看:**链确实是对的,而且我们把前三环都走通了**;
只是终点不是「逼近 vLLM」,而是发现 fp16 下我们本就更快、差距只在 int4 才显形(见归因)。

---

## ✅ P1. FlashAttention —— 已完成(OPTIMIZATION §6)

不自己编译、不撞 nvcc/cu130 的墙:**直接 import vLLM 自带、和本机 torch ABI 匹配的 flash 二进制**
(`vllm.vllm_flash_attn`),零编译。decode 用 flash-decoding(KV 池槽位当一个 paged block,
`block_table` + `seqused_k` 直接读池、不 gather、GQA-native、不物化 KV),prefill 用 varlen。
micro-bench flash vs SDPA+物化 **11.56x**;引擎内 decode 29.6→19.65ms(1.5x)。**19% 的 GQA KV
物化消失。** 复用生产二进制而非和编译器搏斗,是全项目 ROI 最高的一步。

## ✅ P2. CUDA graph 捕获 decode —— 已完成(OPTIMIZATION §7–§8)

当初判「decode GPU-bound,graph 无用(back-to-back/synced=1.02)」。**这个结论被 P 后续推翻。**
先修两处 omniserve 自己的低效:
- **§7 decode-copy fix**:`flash_decode` 每层把整个 `max_len=1024` 的 KV transpose+contiguous,
  实际才 ~300 → 只拷 `ret_len`(16 倍数)。decode **16.4→12.6ms**,顺带证伪了「那 4ms 是 vLLM
  kernel 护城河」——是我们自己的拷贝浪费。
- **§8-A KV layout**:池从 `[B,nkv,max_len,hd]` 改成 `[B,max_len,nkv,hd]`(正好是 flash 的 block
  布局,免每层 transpose),写入改单维 `index_copy_`。decode **12.6→11.5ms**。

修完后即便 CPU 跟得上,**GPU 侧 28 层 kernel 之间仍有调度间隙** → CUDA graph 把间隙吃掉,decode
**11.5→9.5ms(1.2x)**。整条 decode 链 **16.4→9.5ms = 1.72x**,token 一致。

> **★调试坑(以后做 CUDA graph 必看)**:反复 illegal memory access。`mg.py` 隔离 capture
> 单层 / 完整 forward 都 OK → 问题在 engine 集成。真凶:**capture 用的 `view` 是局部变量,函数
> 返回后被 GC**,它被 captured graph 引用的内部 tensor(`_rows` 等)内存被复用 → replay illegal
> access。修复:`self._graphs[n] = (g, buf, view)` 保活。另两坑:① 依赖 buffer 的索引
> (`flat`/`seqused`)必须图外预算成 static buffer;② 不能在 `@torch.inference_mode()` 内 capture。

## ✅ P3. 量化 GEMM(Marlin / GPTQ)—— 已完成(OPTIMIZATION §9)

作为**可选扩展**(默认仍 fp16,公平对比恒 fp16-vs-fp16)。为了和 **vLLM-int4 同精度**对比(不是
int4-vs-fp16 的便宜),omniserve **加载 vLLM 同款生产 GPTQ-Int4 权重**、`gptq_marlin_repack`
成 Marlin 跑(`omniserve/kernels/marlin_linear.py`)。同精度结果:真冷 421 vs 453(0.93x),
reuse0.9 **1785 vs 2186(0.82x)**——经 §7/§8 从 0.54x 一路追上来。

> 坑:① 每层两个 RMSNorm 必须加载(留初始化全 1 → 整体乱码,proj 单测却全过,逐层二分定位);
> ② **bias 不能传进 `gptq_marlin_gemm`**(结果错 rel 0.90),要 kernel 外手动加(rel 0.0002)。

**自家在线 RTN 只能量化 MLP**(qkv/o 对量化敏感,RTN 会乱码且小 GEMM 本就不快);要全 decoder
同精度必须用校准过的 GPTQ 权重。

---

## ⏳ P4. varlen / 混合 prefill —— 真正剩下的 omniserve 自身 TODO

**现状**:打包 prefill 的调度器预算 + PackedPrefillCache + block-diagonal mask 路径已写好(token
一致),但注意力是 O(total²),实测 0.97x(没收益)。**就差把注意力换成 varlen flash
(`cu_seqlens`,只算段内、零浪费)**,打包 prefill 才真正提速;再进一步做 prefill 块 + decode
混在一个 forward(vLLM 的 chunked+mixed)。P1 的 flash 二进制已支持 varlen,主要是集成 + 验证。

这是 fp16 也能拿到的收益(cold-prefill 大头),且不依赖任何「追平 vLLM kernel」的硬骨头。

---

## 已不再追的(带数据的「不做」决定)

- **fused decode kernel(norm+rope+residual+quant 融合成更少 kernel)**:这是 int4 decode
  9.5ms vs vLLM ~7–8ms 那最后一截差距的来源,纯 vLLM 几十人年 kernel 工程。measure 已证
  `VLLM_MARLIN_USE_ATOMIC_ADD`、再加 graph 都不再帮——收益不值在 8GB 上复刻。
- **重叠批量 prefill(批 ViT + prefill/decode 流水)**:int4 真冷 vLLM 290→453 靠这个,
  omniserve 卡在串行逐图 ViT。同属 vLLM 引擎成熟度,不追。
- **lean C++ forward**:一次重写,相对上面收益边际,不做。

## 归因(本项目最有价值的判断,见 OPTIMIZATION.md「The attribution」)

差距不在固定一层,**随精度迁移**:
- fp16:decode 卡**带宽 floor**(硬件、两边平等)、prefill 卡 **ViT**(两边接近)→ omniserve
  精简路径**赢**。
- int4:权重读取掉到 1/4,瓶颈从带宽换成 **kernel 调度效率**(vLLM 护城河);prefill 被 vLLM
  量化 LLM + 重叠吃满 → omniserve **被反超**。

**量化没让 omniserve 变慢,是让 vLLM 的工程优势变可见。** 能指出差距在哪一层、为什么随精度移动、
要什么(融合 kernel + prefill 重叠)才能补——比刷一个数字强。
