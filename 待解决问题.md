# 待解决问题 / 下一步攻坚

记录把 omniserve 从 **0.44x of vLLM** 往上推需要解决的问题。按「复利链」排序——
nsys 证明这是一条链,**第一环不解决,后面的都白做**(见 OPTIMIZATION.md「Why vLLM
is fast」)。每条标注:为什么、方案、难点、依赖、怎么验证。

```
复利链(vLLM 的快是层层相乘的):
  P1 FlashAttention ──► P2 CUDA graph(因为变 launch-bound 才有用)
        │              └─► P4 varlen/混合 prefill(需要 cu_seqlens)
        └─► 顺带干掉 P 的 19% KV 物化
  P3 量化 GEMM(独立的另一条线:减权重字节)
```

---

## P1. 接入 FlashAttention —— 最高优先级,复利链的第一环 ⭐

**问题**:decode 注意力用的是 PyTorch SDPA(`fmha_cutlassF`),带来两个连锁问题:
1. **GQA KV 物化**:每层把 `[B,2,L,128]` 用 `repeat_interleave` 扩成 `[B,12,L,128]`
   写显存,nsys 实测占 **decode 的 ~19%**。SDPA 给不了 GQA-native(试过 `enable_gqa`
   反而更慢,会退到慢的 math 后端)。
2. **不够快 → GPU-bound** → 导致 P2 的 CUDA graph 失效(我们实测 graph 在当前 omniserve
   上没用,根因就在这)。

**为什么第一**:vLLM 用 `flash_fwd_splitkv`(FlashAttention 的 decode 版),GQA-native、
快;它让每步 GPU 时间短到变 launch-bound,P2 才生效。**整条链都挂在这一环上。**

**方案(二选一)**:
- (a) 装 `flash-attn` 预编译 wheel(匹配 torch 2.9 + cu128),decode 用 flash-decoding
  / `flash_attn_with_kvcache`,prefill 用 `flash_attn_varlen_func`(cu_seqlens)。
- (b) **自写 Triton FlashAttention**(decode 单 query splitkv + prefill varlen)。

**难点 / 风险**:
- (a) 之前装 sglang 时撞过 **torch cu130 不兼容驱动 570、nvcc 缺失** 的墙;要找到匹配
  cu128 的预编译 flash-attn(避免现场编译)。
- (b) 周级工程,但**可控、不依赖编译环境**,而且是顶级 kernel 工件。
- GQA + M-RoPE 已应用 + 预分配池的 KV 布局要对接好。

**验证**:token 一致 → decode 提速 → 重新 nsys 确认 ① KV 物化消失 ② 变成 launch-bound
(back-to-back/synced 比值 < 1)。

---

## P2. CUDA graph 捕获 decode step —— 依赖 P1

**问题**:现在 decode 是 GPU-bound,CUDA graph(消 launch 开销)没用——实测 back-to-back
与 synced 比值 1.02。

**依赖**:**P1 完成后**,FlashAttention 让 decode 变 launch-bound,graph 才有意义
(vLLM 实测 graph 单独给 **1.7x**:293→503 tok/s)。

**方案**:固定 batch/形状,捕获 decode step 的 CUDA graph 回放。预分配 KV 池已经给了
固定显存地址(graph 需要),这点已就绪。

**难点**:continuous batching 的动态 batch(序列退休)→ 要么捕获几个固定 batch size 各
一张图、实际 padding 到最近的;要么固定 batch。自写 forward 可控。

**验证**:decode 提速接近 vLLM 的 1.7x 量级;nsys 看 launch 开销是否被回放吃掉。

---

## P3. 量化 GEMM(Marlin / FP8)—— 独立的带宽杠杆

**问题**:decode 的 **GEMM 占 55%**,在带宽 floor(fp16 2B 权重每步读 ~4.4GB)。fp16
kernel 榨不动;bnb 4-bit 实测**反而慢 0.84x**(反量化没融合)。

**方案**:
- **Marlin**:4-bit 融合 dequant+GEMM kernel,真的只读 1.1GB,接近 4x decode 加速。
- **FP8**:sm_89(你的 4060)原生 FP8 tensor core,权重 2→1 字节,硬件加速。

**难点**:Marlin kernel 集成 + 4-bit 量化权重加载;FP8 需 FP8 GEMM 路径 + 量化校准。
都不是 HF/bnb 自带的(又是 vLLM 护城河)。

**验证**:decode 提速 + 输出质量(量化有精度损失,看 token 接受度 / perplexity)。

---

## P4. varlen / 混合 prefill —— 依赖 P1

**问题**:打包 prefill 现在用 block-diagonal mask,注意力 O(total²),实测 0.97x(没收益)。

**依赖**:**P1 的 varlen FlashAttention(cu_seqlens)**——只算段内、零浪费,打包 prefill
才真正提速;再进一步做 prefill 块 + decode 混在一个 forward(vLLM 的 chunked+mixed)。

**现状**:打包 prefill 的调度器预算 + PackedPrefillCache + block-diagonal 路径已写好
(token 一致),就差把注意力换成 varlen。

---

## P5. 环境 / 安装(贯穿 P1、P3)

flash-attn、Marlin 都需要 CUDA toolkit / nvcc / 匹配的 cu128 wheel。踩过的坑:
- 最新 sglang 拉 torch **2.11+cu130**,驱动 570 只到 CUDA 12.8 → `cuda available False`。
- cuda-toolkit 大文件下载被代理中断(IncompleteRead)。
- `kernels` 包和 huggingface-hub 版本冲突。

**对策**:优先找**匹配 torch 2.9.0+cu128 的预编译 wheel**;实在不行就走**自写 Triton**
路线(P1b),绕开编译。环境用独立 conda env,别污染 vllm_bench。

---

## P6.(低优先)lean forward / 减少 Python 开销

自写 forward 仍是 Python 逐层 dispatch。P2 的 CUDA graph 做了之后,这部分开销大半被
回放吃掉,所以**不单独做**,跟在 P2 后面自然解决。

---

## 建议执行顺序

1. **P1 FlashAttention**(先 (a) 找 wheel,1 天内不成就转 (b) 自写 Triton)——解锁整条链;
2. **P2 CUDA graph**——拿 1.7x;
3. **P4 varlen prefill**——顺带(P1 的 varlen 已经有了);
4. **P3 量化 GEMM**——独立的大杠杆,可与上面并行评估;
5. 重新跑 vs vLLM 对比 + nsys,更新 OPTIMIZATION.md 的数字。

**预期**:P1+P2 做完,decode 应能从 ~0.83x 往 1x 逼近,端到端可能到 **0.6–0.7x of vLLM**;
P3 再叠加。剩下的是 PagedAttention kernel 和 C++ 前向那种 vLLM 几十人年的尾巴。
