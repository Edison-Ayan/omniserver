# 逼近 vLLM 的探索日志

记录自主攻坚的尝试、失败、成功。目标:解决 [待解决问题.md](待解决问题.md) 的 P1
(FlashAttention)——复利链第一环。

---

## 第 1 阶段:P1 FlashAttention(进行中)

### 尝试 1:能不能直接复用 vLLM 已装好的 flash?(成功 ✅)

**动机**:自己装 flash-attn 撞过 nvcc/cu130 墙(P5)。但 vLLM 已经在跑 flash,它的二进制
一定和 torch 2.9+cu128 ABI 匹配——能不能直接 import?

- ❌ `import vllm_flash_attn` → 没这个顶层模块。
- ✅ `from vllm.vllm_flash_attn.flash_attn_interface import flash_attn_varlen_func` → **可用!**
  在 `vllm/vllm_flash_attn/` 子包里,ABI 已匹配。**P5 安装墙直接绕过,零编译。**
- 顺带发现 `flashinfer` 也装了(0.5.3),备选。

**经验**:不要急着自己编译 kernel——先翻一遍已装的重型库(vllm/flashinfer)有没有现成
的、ABI 已匹配的二进制可借。省掉整个编译地狱。

### 尝试 2:micro-bench flash vs SDPA+物化(成功 ✅,11.56x)

decode 形状(B=16, nh=12, nkv=2, hd=128, L=350):
```
SDPA + repeat_interleave(当前): 456.6 us/次
flash_attn_varlen_func:          39.5 us/次   ← 11.56x,误差 0.0002
```
**flash 一举干掉两块**:① 注意力本身更快;② GQA-native 不用物化 KV(那 19%)。
证实 P1 是对的杠杆。

### 尝试 3:怎么把 pool 的 padding KV 喂给 flash(踩坑 → 成功)

pool 存的是左对齐 padding 的 KV,flash varlen 要 packed。试了几条:

- ❌ `seqused_k` + `cu_seqlens_k` 一起传 → `assert cu_seqlens_k and seqused_k cannot be
  provided at the same time`。两者互斥。
- ❌ K 传 4D `[B,maxL,nkv,hd]` + seqused_k、不分页 → reshape 报错。flash 的 4D K 必须
  配 block_table(分页)才能解释。
- ✅ **分页版**:每个 slot 当一个 block(block_size=max_len),`block_table[seq]=[slot]`,
  `seqused_k=[L_i]`,K cache 布局 `[num_slots, block_size, nkv, hd]`。误差 0.0005。
  **flash 直接读 pool buffer,免 gather!**

**关键参考**:直接看 vLLM 自己 decode 怎么调(`v1/attention/backends/flash_attn.py`)——
它就是 `seqused_k + block_table` 分页,不传 cu_seqlens_k。照抄它的姿势就对。

**经验**:flash 的 API 组合约束很多(varlen/packed/paged/seqused 互相排斥),别瞎试,
直接读生产代码(vLLM)的真实调用是最快的。

### 尝试 4:集成进 decode(成功 ✅,decode 1.5x)

**做法**(不破坏 HF runner / 不动 pool 布局):
- `_CacheView.flash_decode(q, k_new, v_new, layer, scale)`:新 token 原地写进池 →
  把 `pool.k[layer][:n]` 从 `[slot, nkv, max_len, hd]` 转置成 flash 的 block 布局
  `[num_blocks, max_len, nkv, hd]`(.transpose+.contiguous)→ `block_table=[[slot]]` +
  `seqused_k=有效长度` 调 flash。
- `Attention.forward`:decode(cache 有 `flash_decode`)走 flash;prefill 仍 SDPA。

**结果**:
- **decode 19.65 ms vs 29.6 ms = 1.5x(541 → 814 tok/s)**。
- token:vs HF 3/4 完全一致,1/4 语义等价(flash 的 fp16 数值偶尔翻一个词,和
  vLLM vs HF 同类差异,可接受)。

### 反直觉发现:flash 后仍是 GPU-bound,CUDA graph 还是上不了 ⚠️

测 back-to-back vs synced,比值仍 **1.00** → **还是 GPU-bound**。flash 把 decode 砍到
19.8ms,但 **GEMM(~16ms)现在占 82%**——flash 没碰 GEMM。

**修正复利链的理解**:我原以为"flash → launch-bound → CUDA graph"。实测:flash 让 GPU
干得少了,但 GEMM(带宽 floor 读权重)仍是瓶颈,decode 还是 GPU-bound。**P2 CUDA graph
依然上不了;下一个真正的杠杆是 P3 量化 GEMM(减权重字节)。** vLLM 能吃到 CUDA graph 的
1.7x,是因为它的 GEMM 还更快(可能也量化/更优 tiling),把 GPU 时间压到比 CPU 发射还短。

### P1 小结

- ✅ **复用 vLLM 自带 flash,零编译**(绕过 P5 安装墙)——最大的省事点。
- ✅ **decode 1.5x**,GQA KV 物化(19%)+ 慢注意力(20%)两块一起干掉。
- ⚠️ 但没解锁 P2(仍 GPU-bound,GEMM 成新瓶颈)→ 下一步应转 **P3 量化 GEMM**,不是 P2。
- 端到端 vs vLLM:
  - **decode-heavy(gen=200):0.51x → 0.68x of vLLM**(337→451 tok/s,+34%)← flash 真实威力
  - prefill-heavy(gen=64):0.44 → 0.47x(被串行 prefill 稀释)
  - decode-only:541 → 814 tok/s(1.5x)

**P1 是目前最大单点提升。**

---

## 第 2 阶段:P3 量化 GEMM(FP8,部分成功但 lossy)

### 尝试 1:能不能复用 vLLM 的 Marlin?(转向 FP8)

- vLLM 的 `torch.ops._C` 量化 op 没直接搜到好用的入口。
- **但发现 `torch._scaled_mm` 原生存在,GPU 是 sm_89 有原生 FP8 tensor core** → 不用 vLLM,
  PyTorch 自带 FP8 GEMM。转向 FP8(比 Marlin 的 int4 打包简单)。

### 尝试 2:FP8 micro-bench(成功 ✅,大 GEMM 4.91x)

最大的 gate_up GEMM(K=1536,N=17920):FP8 `_scaled_mm` **4.91x**(权重 55→28MB +
FP8 tensor core)。坑:`_scaled_mm` 要 mat2 列主序(正好 nn.Linear 的 weight.t());
scale 要 float32 singleton;RowWise(per-channel)只支持 bf16 输出。

### 尝试 3:加上动态激活量化后,小 GEMM 反而慢(踩坑 ⚠️)

`_scaled_mm` 要求**两个操作数都是 FP8**,所以激活每步要动态量化。即使融合成一个 Triton
kernel(amax+cast),固定开销也只有最大的 GEMM 摊得平:

| GEMM | 加速 |
|---|---|
| gate_up (N=17920) | **2.93x** ✅ |
| down (K=8960) | 0.40x ❌ |
| qkv / o (小) | 0.26-0.34x ❌ |

**教训**:FP8 适合 prefill(大 M、compute-bound)。decode(小 M)上激活量化开销甩不掉,
只有最大的 gate_up 值得 FP8。这正是为什么 vLLM 用 **Marlin(int4 权重 + fp16 激活,无
激活量化)** 做 decode 量化,而不是 FP8。

### 尝试 4:只 FP8 化 gate_up(部分成功,但质量降)

gate_up 占每层 GEMM 时间 ~83%,只 FP8 它、其余 fp16:
- **decode 19.65 → 16.71 ms(+10%,814→898 tok/s)**。
- **质量:vs fp16 只 1/3 完全一致**(FP8 e4m3 ~3.7%/GEMM,28 层累积,输出仍连贯但分叉)。

### P3 小结

- ✅ FP8 `_scaled_mm` 零依赖(PyTorch 原生 + sm_89)、大 GEMM 快。
- ⚠️ 但 decode 上被激活量化开销限制(只 gate_up 划算)+ **质量有损**(比 flash 重)。
- **净收益 +10% decode,代价是质量**——比 P1 flash 弱很多。
- FP8Linear 作为**可选工具**留下(默认不开)。**真正无损的 decode 量化要 Marlin
  (int4 + GPTQ 校准),是更大的工程**,留作 P3b。

下一步建议:**P4 prefill 接 flash varlen**(无损、decode 已铺好 flash 复用)比继续榨
量化更划算。

---

## 第 3 阶段:P4 prefill + 「能否到 vLLM 90%」的诚实评估

### P4:prefill 接 flash varlen(无损,但只 +11%)

打包 prefill 注意力换成 flash_attn_varlen_func(cu_seqlens 段内因果),去掉 O(total²)
块对角 mask。无损。但 prefill 只快 ~11%——**因为 prefill 不是注意力受限**。

### 为什么 prefill 卡住:ViT 占 74%,且 GPU-compute-bound

profile:prefill 2619ms 里 **ViT 1941ms(74%,121ms/图)**。试了三条都不行:
- ❌ 批量 ViT(16 张一次)→ 更慢(0.88x):ViT 注意力是 per-segment Python 循环,批量后
  循环 16×32 次,开销更大。
- ❌ ViT 注意力换 flash → 破坏正确性(rel 3.04)且无提速 → 说明 ViT 瓶颈不是注意力。
- ❌ ViT torch.compile → 只 1.17x 且误差 27(reduce-overhead 的 cudagraph 抓到脏数据)。
- 测了 back-to-back/sync 比值 **0.99 → ViT 是 GPU-compute-bound**,不是 launch/Python。
  就是 675M 参数的 ViT 在 4060 上跑 256 patch × 32 block 的真实算力,榨不动。

### ⭐ 能否到 vLLM 90% —— 诚实结论:不能(在这套栈上)

把所有杠杆推完后的天花板:

| 场景 | 当前 | 到 90% 需要 | 可行性 |
|---|---|---|---|
| decode-heavy | **0.68x** | decode 再 1.3x | ❌ decode 82% 是 GEMM 带宽,只有 Marlin(无损 int4)能 close,大工程 |
| prefill-heavy | **0.47x** | prefill 大幅加速 | ❌ ViT(74%)compute-bound,要 fused kernel 重写 ViT,vLLM 几十人年的活 |

**两个瓶颈都撞到了「需要专用 kernel / 无损量化」的硬墙**:
- decode:GEMM 带宽 → 要 **Marlin(int4)** 或 **FP8**(实测有损 +10%);
- prefill:ViT 算力 → 要 **fused ViT kernel**(vLLM 有,我们没有)。

这俩都不是「我的低效」,是 **vLLM 的专用 kernel 护城河**。在 HF/PyTorch/SDPA 这套栈上,
**90% 不可达;诚实的天花板是 decode-heavy ~0.68x、prefill-heavy ~0.47x**。

### 但这恰恰是最有价值的简历结论

不是「我追平了 vLLM」(没人信、也做不到),而是:
> **我用 measure/nsys 把差距精确拆到每一项(decode=GEMM带宽、prefill=ViT算力),
> 把所有结构性 + 可复用 kernel 的优化都做完(5.4x→1.5x),并能说清剩下的 1.5x 为什么
> 必须靠 Marlin/fused-ViT 这种专用 kernel——也知道它们各自的工程量和有损/无损取舍。**

这种「知道极限在哪、为什么、代价多大」的判断力,比一个虚假的 90% 强得多。

---

## 第 4 阶段:nsys 推翻"ViT 算力墙",prefill 再进一步

上阶段我判"ViT 是算力墙、放弃 prefill"。**nsys 剖析 ViT 内部推翻了它:**

```
43.4% GEMM(MLP/qkv) | 24.6% vol2col | 13.4% gemvx | 8.1% flash 注意力
```

**真凶是 patch_embed 的 Conv3d(vol2col+gemvx 共 ~38%,3 万个小 kernel),不是注意力。**
PyTorch 把 `Conv3d(kernel=stride)` 实现得极低效;而它等价于对展平 patch 的一个 matmul。换掉:

- **ViT 逐张 1943 → 1328ms(1.46x);prefill-heavy 0.48 → 0.56x。**
- matmul vs Conv3d 真实输入误差 0.0005,ViT 极敏感放大到原始输出 rel~12,但端到端生成
  vs HF **5 图 4/5 完全一致、相似度 0.99**,语义无损(同 flash 类)。

**教训(measure-first 又一次救场)**:差点错误放弃 prefill;nsys 一看发现是"框架实现低效"
(Conv3d→vol2col),不是算力墙。**别凭直觉判死刑。**

- ❌ matmul 后再批量 ViT 仍 0.84x(注意力段循环 hd=80 用不了 flash,xformers 没装)。

### 截至目前诚实状态

```
prefill-heavy: 0.48 → 0.56x(patch_embed matmul)
decode-heavy:  0.68x(FlashAttention)
```

90% 仍不可达,但 prefill 又挪了一截。剩余:decode=GEMM 带宽(要 Marlin);prefill 的 ViT
GEMM=compute-bound(批量被 hd=80 注意力卡住,或要 fused ViT kernel)。

---

## 第 5 阶段:⭐ 决定性发现 —— 同精度下 decode 其实比 vLLM 快

「vLLM 同精度为什么快」——我用斜率法(gen=64 vs gen=320,消掉 prefill)分别量两边的**纯
decode 边际吞吐**,batch 16、fp16、都走完整引擎:

```
omniserve decode 斜率: 709 tok/s
vLLM       decode 斜率: 535 tok/s
=> omniserve decode 比 vLLM 快 1.32x
```

**结论反转:对 decode,vLLM 不快——omniserve 更快。** batch 16 时 decode 访存受限、两边都
在带宽 floor,而 omniserve 的精简 flash+预分配路径开销比 vLLM 的通用引擎机制小(vLLM 为大
batch 优化,小 batch 非其最优区)。

**所以同精度的整个差距全在 prefill**:
- omniserve prefill 串行 + ViT 逐图,~2.5s;
- vLLM prefill 批量 ViT + chunked prefill 与 decode 重叠。

benchmark 的 0.56/0.68x 是 prefill 主导,把 omniserve 的 decode 优势盖住了。**这才是
「同精度 vLLM 为什么快」的真答案:不是 decode,是 prefill。**

### 项目最终诚实结论

> 8GB 单卡、batch 16、同精度(fp16):**omniserve decode 比 vLLM 快 1.32x**(精简
> flash+预分配 vs 通用引擎开销);**差距全部来自 prefill**(vLLM 批量 ViT + prefill/decode
> 重叠)。差距被精确隔离到单一阶段,且每一步都有 measure/nsys 支撑。

比"追到 90%"诚实、且更有说服力——它说明在它擅长的工况(小 batch、decode-heavy),自写引擎
能赢过生产框架。

---

## 第 6 阶段:继续缩 prefill

**① 只算每段最后 token 的 logits + 启用批量 prefill**(无损):
- 之前批量 prefill OOM,因为对 [total, vocab] 全量算 logits([6864,151936]=2GB);只需每段
  最后一个 token。LLM forward 加 logits_indices。
- **prefill-heavy 0.56 → 0.65x**(280→329 tok/s)。这是 LLM prefill 部分(~26%)打包的收益。

**② 批量 ViT —— 实测不帮忙(已撤回)**:
- 以为 ViT GEMM 小、批量能提效。实测批量 ViT e2e 反而略慢(86 vs 92)。
- 原因:**ViT 处理 1024 patch/图(merge 前),M=1024 的 GEMM 已经喂满 GPU**——不是小矩阵,
  批量不增效。我之前"ViT GEMM 利用率低"的假设错了。ViT 的开销是 1024×32 的真实算力。
- 教训:别假设"批量一定更快",measure 了再说。patch_embed matmul(1.46x)才是 ViT 的真省点。

### prefill 当前状态与剩余

```
prefill-heavy: 0.48 → 0.56(patch_embed)→ 0.65x(批量 LLM prefill)
```
剩余瓶颈是 ViT 的纯算力(M=1024 已饱和,只有 fused ViT kernel 或量化能再压)+ prefill/decode
不重叠(vLLM 的 chunked mixed,需进一步工程)。**prefill 的"结构性 + 可复用"优化已基本做完。**

---

## 第 7 阶段:深入研究 vLLM 如何解决 prefill(实测拆解)

**实测:vLLM prefill(24图,gen=1)939ms vs omniserve 2900ms —— vLLM 快 3x。** 读 vLLM
源码 + nsys 拆出它的三招:

### 招 1:ViT 注意力用 flash_attn_varlen(已照搬)
vLLM 的 `Qwen2VisionAttention` 用 `flash_attn_varlen_func`(cu_seqlens 分段、一次调用),
SDPA per-segment 循环只是 fallback。**我之前以为 hd=80 flash 不支持(rel 3.04)是误判**
——那是 ViT 深层数值漂移,flash hd=80 实测误差 1e-4、生成 5/5 一致。已接上。

### 招 2:批量 ViT(一次 forward 处理所有图)
vLLM 的 `Qwen2VisionTransformer.forward` 把所有图的 patch 拼成一次前向,cu_seqlens 分段。
⚠️ 但我的批量 ViT 实测**反而慢**(0.85x):同样的 ampere s1688 GEMM,我的批量 M=16384
比逐图 M=1024 还慢——这是个我没完全解开的低效(vLLM 的批量 ViT 是快的)。

### 招 3:⭐ ViT 与 LLM-prefill 重叠(流水线)—— 最大的那招
拆解 omniserve 的 2900ms:ViT 1973ms(68%)+ 批量 LLM prefill 926ms(32%),**串行相加**。
vLLM 把两者**重叠**(ViT 算第 i+1 张图时,LLM 在 prefill 第 i 张),total ≈ max(ViT, LLM)
而非相加。这是它 939ms 的关键结构差异。

### 还有:vLLM 的 ViT GEMM 本身约快 ~2x
即使算上重叠,vLLM 的 ViT(<939ms/24图)仍比我的(1973ms/24图)快 ~2x,用的是同一个
ampere s1688 kernel。这个纯 kernel 效率差距我没能解释清(可能是更优的 cutlass 配置/融合/
bf16),属于 vLLM 的成熟工程。

### 结论:prefill gap 的真正成分
```
vLLM 快 3x = 重叠(ViT||LLM, ~省 30%)× ViT GEMM 效率(~2x)
```
- **重叠**:可做(CUDA 流 + 分块流水线),~1.47x,但工程复杂、且受 ViT 主导;
- **ViT GEMM 2x 效率**:vLLM 的成熟 kernel 工程,难复刻。

**这是「vLLM 如何解决 prefill」的完整答案:不是单点,是 flash-varlen ViT + 批量 ViT +
ViT/LLM 流水线重叠 + 更优 GEMM 配置 的组合。** 我能照搬招 1,招 2/3 是结构性工程,招 4 是
kernel 护城河。

### ⚠️ 修正:招3「重叠」是误判(measure 证伪)

准备实现招3前先测了前提:**单 GPU 上两个 compute-bound GEMM(ViT 和 LLM 都是)放不同
CUDA stream 并发 = 1.00x,完全不重叠**——它们抢同一批 SM,只是时分复用。所以:

- **招3(ViT/LLM 流重叠)在单 GPU 上是死路**,不会加速。差点实现一个没用的复杂流水线,
  measure-first 又救了一次。
- 这也**推翻了我对 vLLM 939ms 的"重叠"解释**:vLLM 不是靠流重叠(单卡没用),而是
  **ViT 本身就快 ~2x**(招4,GEMM 效率)。omniserve ViT 1973ms/24图 > vLLM 全 prefill
  939ms,所以 vLLM 的 ViT 单独就 ≥2x 快,用同一个 ampere s1688 kernel。

### prefill 的最终诚实结论
prefill gap **本质是 vLLM 的 ViT GEMM 效率(~2x,同 kernel 但更优配置/批量/可能 bf16)**
——纯 kernel 工程护城河,不是结构问题,我没能复刻。我已做完所有结构性 + 可复用优化
(patch_embed matmul、flash-varlen ViT、批量 LLM prefill),prefill-heavy 到 0.65x。
**再往下是 vLLM 几十人年的 kernel 工程,诚实地说,在这个栈上到顶了。**

> ⚠️ **本结论在第 8 阶段被 measure 推翻——见下。** "ViT GEMM 2x 护城河"是误判,
> 根因是基准测试的 prefix-cache 不对称。

---

## 第 8 阶段:⭐⭐ 推翻"prefill gap 3x"——它是基准的 prefix-cache 假象

phase 7 判 prefill gap = vLLM ViT GEMM 快 2x(kernel 护城河)。但那是**推断**(从
"vLLM 全 prefill 939ms < omniserve ViT 1973ms" 反推),从没直接量过 vLLM 的 ViT。
这次直接 head-to-head,全在同一台 4060、同 fp16、同一个进程里测。

### 实测 1:vLLM 的 ViT 根本不快(推翻 2x)

直接 import `vllm...Qwen2VisionTransformer`,随机权重(只测时间),喂同样 1024 patch/图:

```
vLLM ViT      66.2 ms/图
omniserve ViT 72.9 ms/图     ← 只差 1.10x,不是 2x
批量 24 图:两边都 ~74 ms/图  ← M=1024 已喂满 4060,批量零收益(0.92x,反而略慢)
```

**没有 2x kernel 护城河。** 两边用同一个 cuBLAS + 同一个 flash(omniserve 本来就 import
vLLM 的 flash)。ViT 分解:GEMM 占 77%,且 M=1024×24 == M=24576×1 = 1.00x(GEMM 已在
cuBLAS peak,无可榨)。phase 7 的"同 kernel 但 2x"在物理上就不成立。

### 实测 2:⭐ 根因——基准对 prefix cache 双标

- `run_vllm.py`:预热 `generate(同一批图)` → 计时 trial 重跑**同一批图**。vLLM v1
  **默认开 prefix caching**(`enable_prefix_caching=True`)→ 计时 trial 全部命中前缀缓存、
  **整段 ViT+prefill 被跳过**,只剩 decode。
- `run_omniserve.py`:每个计时 trial 前 `reset_caches()` → **每次冷跑完整 ViT**。

一次加载 vLLM,gen=1 隔离 prefill,实测:

```
vLLM 暖(同图,前缀命中):  0.93 s   ← 这就是日志里的"939ms"!是暖数,不是真 prefill
vLLM 冷(没见过的图):      2.89 s   ← 真·prefill ≈ omniserve 冷 ~2.9s,持平
```

**"939ms vs 2900ms = 3x" 是拿 vLLM 暖 ↔ omniserve 冷比,苹果比橘子。** 都冷的话 prefill 持平。

### 实测 3:公平的端到端(都冷,gen=64,24 张不同图,同台机器)

```
vLLM 暖(= run_vllm.py 测的):   502 tok/s  (≈ vllm.json 504,确认它是暖/前缀命中数)
vLLM 冷(真 prefill):           287 tok/s  (wall 4.62s, 1327 tok)
omniserve native fp16 冷:       340 tok/s  (wall 4.37s, 1488 tok)  ← omniserve 反而快 1.18x
```

基准里的 **0.34x(172 vs 504)是双重不公**:① vLLM 暖(504)对 omniserve 冷;② omniserve.json
用的还是慢配置(172,旧 4bit / 非 native),不是 native fp16 的 340。

### 第 8 阶段结论(修正 phase 5/7)

1. **公平测量下 prefill 持平、端到端 omniserve 还略快(1.18x)。** phase 7 的"ViT GEMM 2x
   护城河"是误判——根因是 prefix-cache 不对称,不是 kernel。又一次 measure 救场:这次救的
   是别把自己的引擎冤枉成"慢 2x"。
2. **vLLM 真正赢的地方只有一个:前缀缓存命中**(reuse 0.9 → vllm_r9 = 1374 tok/s)。而
   omniserve 的 prefix cache **流内不命中**(omni_pc_r9 = 346,几乎零增益)——一批请求同时
   入队、prefill 在任何请求写回缓存前就全做完了,所以命中不了。**这才是要修的真杠杆。**

### 减小差距的行动项(按收益排序)

- **P0 修基准对称**:vLLM 也 `reset` / 关 prefix caching,或 omniserve 也开 `--prefix-cache`
  且不 reset。再用 native fp16(不是 172 那个旧配置)。光这一步报告数就从 0.34x → 1.18x。
- **P1 修 omniserve prefix cache 让它流内命中**:reuse 高时 vLLM 靠它吃到 4x,omniserve 现在
  吃不到。让相同(prompt+图)的请求在同一 step 内复用先到者的 ViT/KV(或 ViT embedding 先
  去重再 prefill)。这是 omniserve 唯一真落后的点。
- **P2(可选)ViT 那 1.1x**:vLLM 的 rope 走 flash 的 `apply_rotary_emb` CUDA kernel,
  omniserve 的 triton rope(57us/block)+ 5 次 .contiguous(33us/block)略重;flash 注意力
  两边同。能抠几个点,但不是大头。

---

## 第 9 阶段:落地 P0(公平基准)+ P1(把 prefix cache 接到快路径)

### ⚠️ 先纠一个第 8 阶段的误判
第 8 阶段说"omniserve prefix cache 流内不命中(omni_pc_r9=346 零增益)"——**那是看了 stale
JSON(hit_rate=null 的旧文件)的误读**。重查 `omni_pc_0.9.json`:hit_rate=0.917、322 tok/s
vs base 178 = **1.8x,其实命中得好好的**。真正的问题是:**prefix cache 只长在慢的 HF
`QwenVLRunner` 上**(322 tok/s 即便 90% 复用也 < native fp16 冷的 340),而快的 native
runner 根本没接 cache。所以 P1 不是"修命中",是"把 cache 接到快路径"。

### P0:基准对称(run_vllm.py)
- 加 `--no-prefix-cache`(关掉 vLLM 默认开的前缀缓存);
- 前缀缓存开着时,**每个计时 trial 前 `llm.reset_prefix_cache()`**,和 omniserve 的
  `reset_caches()` 对称——不再跨 warmup/trial 白嫖 prefill。

### P1:prefix cache 接到 native runner(快路径)
- 池加 `snapshot_slot / restore_slot / copy_slot_kv`(kv_prealloc.py):槽位 KV 的克隆快照、
  恢复、槽位间直拷。
- native prefill 重写成"规划三类"(qwen_vl_native.py):
  - **compute**:本 batch 首次见到这个 (prompt+图) 前缀 → 真跑 ViT+LLM,算完 snapshot 进 cache;
  - **dup**:同一 batch 内重复前缀 → 从已算的兄弟槽位 `copy_slot_kv`,**不重算**(解决"一批
    同时入队、谁都还没写回 cache"的流内复用——这正是 HF runner 靠逐序列 put-then-get 拿到、
    而打包 prefill 拿不到的);
  - **hit**:跨 step 命中 → 从快照 `restore_slot`。
- run_omniserve.py:`--prefix-cache` 现在也接 native(`--vision-cache` 在 native 下不支持,
  原生 ViT 无 monkeypatch 挂点)。

### 实测(native fp16,24 请求,3 trial 中位)

```
no-cache:              340 tok/s   (基线)
+pc, reuse 0:        341.5 tok/s   命中率 0   ← 无复用零开销(cache 不偷懒)
+pc, reuse 0.9:      821.5 tok/s   命中率 0.917 ← 2.4x
```
正确性:reuse 0.9 下 +pc 的输出和 no-cache **逐 token 完全一致**(greedy 确定性,22 命中/2 算)。

### ⚠️ 又一个陷阱:vLLM 有**两层**缓存,关一层还不够冷
做 P0 时发现:`run_vllm.py --no-prefix-cache` reuse 0 给 **498 tok/s**,但 warmcold 里真冷
(没见过的图)只有 287。差距来源——**vLLM 默认还开着第二层:mm processor/encoder cache
(`mm_processor_cache_gb=4`)**。计时 trial 用的是和 warmup 相同的图,即使关了 prefix KV
cache,ViT 的 encoder 输出仍被复用 → 跳过 ViT。**只关 prefix cache 不够冷。**
真·冷必须让每个 trial 用**全新的图**(任何缓存都命不中)。实测每 trial 全新图:
vLLM 真冷 = **290 tok/s**(289/290/296,稳定),确认 287 那个数,498 是 mm cache 命中。

### 公平对比(gen=64,24 请求,同台 4060,fp16)

```
                          reuse 0 (真冷)         reuse 0.9 (流内复用)
omniserve native(+pc)     341 tok/s              821 tok/s   命中0.917, 逐token一致
vLLM                      290 tok/s (每trial新图) 1325 tok/s  (pc+reset, 流内命中)
                          omniserve 快 1.18x      vLLM 快 1.61x
```
- **真冷:omniserve 反而快 1.18x**(341 vs 290)——确认第 8 阶段,自写引擎裸速度不输 vLLM。
- **高复用:vLLM 还快 1.61x**(1325 vs 821)。此时 prefill 大部分被两边各自的缓存跳掉,负载
  变成 decode-heavy;vLLM 的 **full CUDA graph decode** 是 omniserve 没有的(decode GPU-bound,
  graph 上不了,见 phase 1)。omniserve 的 dup 路径还带 28 层 KV 拷贝开销。这是剩下的真差距,
  在 decode 不在 prefill。

### 第 9 阶段结论
- **P0**:报告数从假的 0.34x 变成真实对比——真冷 omniserve 略快(1.18x),高复用 vLLM 快(1.61x)。
  并发现 vLLM **两层缓存**(prefix KV + mm/encoder),公平冷测必须用"全新图"绕开两层。
- **P1**:把 prefix cache 接到 omniserve 的**快路径**(native runner),reuse 0.9 → **2.4x
  (341→821)**、零复用零开销、逐 token 一致。补上了第 8 阶段认定的"唯一真落后点"。
- **剩余差距换了位置**:不再是 prefill(已持平/略胜),而是 **decode-heavy 高复用下 vLLM 的
  CUDA graph**。下一步若要追,是 P2 = decode 的 CUDA graph / 减少 dup 拷贝开销,不是 prefill。
