# omniserve 运行指南与流程

本文讲三件事:**环境准备**、**怎么启动**、**整个项目的运行流程**。

---

## 一、环境准备

### 1. Python 环境与依赖

项目在 conda 环境 `vllm_bench` 下开发(Python 3.10)。核心依赖:

| 用途 | 包 |
|---|---|
| 计算 | `torch`(2.9+cu128,需匹配你的 CUDA 驱动)|
| 分词 | `tokenizers`(独立于 transformers)|
| 权重加载 | `safetensors` |
| 视觉/图像 | `Pillow`、`numpy` |
| 自写 kernel | `triton`(3.5+)|
| HTTP server | `fastapi`、`uvicorn` |
| 量化(可选 4-bit)| `bitsandbytes` |
| 对比/参照 | `transformers`(只 HF runner 和 benchmark 用)、`vllm`(只对比用)|

```bash
conda activate vllm_bench
# 缺啥补啥(示例)
pip install torch tokenizers safetensors pillow numpy triton fastapi uvicorn bitsandbytes
```

> **native 路径(零 transformers)** 只需要:torch、tokenizers、safetensors、PIL、numpy、triton。
> `transformers` 仅 `QwenVLRunner`(HF 参照后端)和 benchmark 对比脚本需要。

### 2. 模型权重

需要 **Qwen2-VL-2B-Instruct** 的权重在本地 HuggingFace 缓存里:
```
~/.cache/huggingface/hub/models--Qwen--Qwen2-VL-2B-Instruct/snapshots/<hash>/
  ├── model-00001-of-00002.safetensors   # 权重(native 路径直接读这个)
  ├── model-00002-of-00002.safetensors
  ├── tokenizer.json                      # tokenizer
  └── ...
```
国内下载用镜像:`HF_ENDPOINT=https://hf-mirror.com huggingface-cli download Qwen/Qwen2-VL-2B-Instruct`。

> 跑的时候建议设 `HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1`,纯离线、不碰网络。

---

## 二、怎么启动(三种方式)

### 方式 A:作为库,自写 forward(零 transformers,推荐)

```python
from PIL import Image
from omniserve import LLMEngine, Request, SamplingParams, SchedulerConfig
from omniserve.runners.qwen_vl_native import NativeQwenVLRunner

runner = NativeQwenVLRunner(max_running=8)          # 自己的 ViT+LLM,safetensors 加载
engine = LLMEngine(runner, SchedulerConfig(max_running=8))
engine.add_request(Request(
    prompt="描述这张图。",
    images=[Image.open("a.png")],
    sampling=SamplingParams(max_new_tokens=64)))
print(engine.run_until_complete())                  # {request_id: 生成文本}
```

### 方式 B:OpenAI 兼容 HTTP server

```bash
HF_HUB_OFFLINE=1 python -m omniserve.server --port 8000 --prefix-cache   # 加 --fp16 用 fp16
```
然后像调 vLLM 的 server 一样(图片用 base64 data URI):
```bash
curl localhost:8000/v1/chat/completions -H 'Content-Type: application/json' -d '{
  "messages": [{"role": "user", "content": [
    {"type": "image_url", "image_url": {"url": "data:image/png;base64,<...>"}},
    {"type": "text", "text": "这张图里有什么?"}]}],
  "max_tokens": 64, "stream": true }'
```
`"stream": true` 返回 SSE 流式;不带则返回一个完整 `chat.completion`。并发请求由引擎
做 continuous batching。
> server 目前用的是 HF 的 `QwenVLRunner`(参照后端)。

### 方式 C:跑 benchmark / 对比

```bash
# vision/prefix cache 的复用率扫描 + 出图
cd benchmarks && HF_HUB_OFFLINE=1 python bench.py --sweep --requests 16 --out results.csv && python plot.py results.csv
# 对比 vLLM(分进程跑,各自占大半显存)
cd benchmarks/compare && python compare.py --requests 24 --trials 3
# 剖析引擎时间花在哪(prefill vs decode vs 开销)
cd benchmarks && HF_HUB_OFFLINE=1 python profile_engine.py --requests 24
```

### 无 GPU 也能跑控制平面(测调度/流式)

```python
from omniserve import LLMEngine, Request, StubRunner
e = LLMEngine(StubRunner()); e.add_request(Request(prompt="hi"))
print(e.run_until_complete())   # StubRunner 吐假 token,不碰 GPU
```

---

## 三、整个项目的运行流程

### 组件总览

```
HTTP 请求 (OpenAI 格式)            server.py
      │  解析成 Request(prompt + 图)
      ▼
  LLMEngine.step() 循环            engine.py
      │
      ▼
  Scheduler 决定这步跑谁           scheduler.py
      │  (准入新序列做 prefill / 其余做 decode / 退休跑完的)
      ▼
  Runner 执行模型                  runners/qwen_vl_native.py(自写)或 qwen_vl.py(HF)
      ├─ tokenizer + 图像预处理 + M-RoPE 位置   model/tokenizer.py, preprocess.py, positions.py
      ├─ ViT 视觉编码器 + LLM forward            model/qwen2_vit.py, qwen2_llm.py
      ├─ 预分配 KV 池(原地写,无 O(L²) 重建)   cache/kv_prealloc.py
      └─ vision cache / prefix cache            cache/vision.py, prefix.py
```

### 一个请求的生命周期

1. **提交**:`engine.add_request(req)` → 立刻调 `runner.tokenize(seq)`:
   - 图像预处理(smart-resize → 归一化 → patchify)得到 `pixel_values` + `grid_thw`;
   - tokenizer 套 chat 模板,把图像占位符展开成对应数量的 vision token,得到 `input_ids`;
   - 序列进调度器的 `waiting` 队列。

2. **调度**(每个 `step()` 开头):`scheduler.schedule()`
   - 退休上一步跑完的序列;
   - 在 batch 容量/ token 预算内,从 `waiting` 准入新序列去 **prefill**;
   - 其余在跑的序列去 **decode**。

3. **Prefill**(新序列,`runner.prefill`):
   - ViT 把图编码成 vision embedding,拼进文本 embedding;
   - 算 M-RoPE 3D 位置;
   - LLM forward 跑整段 prompt,把 KV **写进预分配池的槽位**;
   - 取最后一个 token 的 logits → 采样出第一个生成 token。
   - (可选)prefix cache:相同前缀直接 clone 缓存 KV、跳过整段 prefill。

4. **Decode**(在跑的序列,`runner.decode`):
   - 所有在跑序列**打包进一次前向**(批量 decode);
   - 新 token 的 KV **原地写**进各自槽位(不重建);
   - 各自采样出下一个 token。

5. **流式输出**:`engine.step()` 把这步每个序列新产生的 token detokenize 成文本增量
   (`StepDelta`),server 通过 SSE 发给客户端。

6. **结束**:序列命中 EOS 或达到 `max_new_tokens` → `runner.free(seq)` 回收它的池槽位
   (把最后一个活跃序列移过来紧凑化)→ 从引擎注册表删除。

### 两个阶段的特点(为什么这么设计)

- **Prefill** 计算受限:一次过几百个 token,算术强度高;贵在 ViT + 长序列注意力。
- **Decode** 访存受限:每步只生成 1 个 token,但要把 ~4.4GB 权重读一遍;贵在带宽。
  → 所以 decode 用**批量 + 预分配池**把多个序列摊到一次权重读里,是吞吐的关键。

更深入的性能分析、对 vLLM 的对比、以及踩过的坑见 [OPTIMIZATION.md](OPTIMIZATION.md)
和 README 的「踩坑与经验」。
